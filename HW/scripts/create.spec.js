import {a} from './create';

describe('General Test', () => {
  describe('Function a', () => {
    it('should return number', () => {
      expect(a()).toBeInstanceOf(Number);
    });
    it('should return number', () => {
      expect(a()).toBeInstanceOf(Number);
    });
  });
});
